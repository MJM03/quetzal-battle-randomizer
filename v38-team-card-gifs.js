/* Legacy V38 disabled — V40 handles team-card GIFs directly on the original sprite. */
(function(){
  // Intentionally empty. Keeping this file avoids breaking cached installs that reference it.
})();
